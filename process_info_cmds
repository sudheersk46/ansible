ps -auxf | grep firefox

pidof firefox
pidstat -p pid
pmap pid | head -1

ls -la /proc/pid/exe

cat /proc/pid/cmdline
 ps -eo pid,lstart,cmd

Pgrep appname

Pkill video




Kill -s sihup pid # reload process

Kill -s sigusr1 pid # show the progress of cmd
